---
name: refactor-daily-forms
description: Dated refactoring process forms with complete, to-do, blocked, and note lines
metadata:
  type: project
  updated: 2026-06-18
---

# Refactor Daily Forms

This file is the single process checklist for completed work and next actions.
Each date gets one form. Every form line must start with one of these labels:

- `complete` - finished and verified as far as the current environment allows
- `to do` - planned next work
- `blocked` - cannot proceed until an external condition changes
- `note` - context that future agents should preserve

## Form Template

| Label | Scope | Line Item | Evidence / Next Action |
|---|---|---|---|
| complete |  |  |  |
| to do |  |  |  |
| blocked |  |  |  |
| note |  |  |  |

## 2026-06-17 Form -- Web Answer Fallback, Checkpointer Race, Query Expansion Design

| Label | Scope | Line Item | Evidence / Next Action |
|---|---|---|---|
| complete | LLM / prompt | **Stable-facts exception in `AGENT_SYSTEM_PROMPT`.** LLM was over-searching for well-known historical facts (e.g. "When was University of Melbourne founded?"), then `web_answer` returned a hard refusal because the fetched pages didn't contain the answer. Added an explicit "answer directly" bullet for founding dates of major institutions, widely-known historical events, capitals of countries, names of public figures, and similar long-settled encyclopedic facts. | `src/llm/prompts.py`: extended the existing "answer directly from your own knowledge ONLY for" list; tightened the trailing "in doubt, USE THE TOOL" line so it does not override the new exception. |
| complete | Graph / lightweight fallback | **`web_answer` no-readable-content fallback to agent.** Previously the lightweight graph terminated at `END` with the grounded refusal whenever no fetched page yielded readable text — even when the LLM already knew the answer from training data. Added a one-shot retry: when `web_answer` produces no readable content, route back to `agent` so it can synthesize from training data with a "couldn't verify against the live web" caveat. Bounded by `WEB_ANSWER_FALLBACK_MAX_ATTEMPTS=2` so a second failure terminates the graph with the original refusal. | Added `web_answer_attempts: int` and `web_answer_no_readable_content: bool` to `RAGState` in `src/graph/state.py`. `src/graph/nodes/web_answer.py` increments the counter on every entry and sets/clears the flag on the failure/success branches. New `route_after_web_answer` and `WEB_ANSWER_EDGE_MAP` in `src/graph/edges.py`. `src/graph/builder.py`'s `build_lightweight_graph` now uses a conditional edge after `web_answer` instead of `add_edge("web_answer", END)`. |
| complete | Tests / lightweight fallback | **8 new tests for `web_answer` fallback behavior.** | `tests/test_lightweight_graph_web_answer_integration.py`: state-field-setting (failure / success / counter increment), edge routing (first-failure / max-attempts / success), and two end-to-end graph tests (fallback retry succeeds; loop bound terminates after two failures). |
| complete | Sessions / checkpoint race | **`SQLiteMemorySaver` thread-safety fix.** Production crash report (`powershell problems.txt` in repo root) showed `RuntimeError: dictionary changed size during iteration` from `_plain_writes` when two concurrent `/chat` requests for the same thread ran the graph invoke. Root cause: the existing `threading.Lock` only wrapped `_persist_state`'s SQLite write, not the inherited `super().put` / `super().put_writes` mutation of `self.writes`. Two threads could be mid-`super().put` and mid-`_plain_writes` at the same instant. Switched to `threading.RLock` and scoped it around the entire put/put_writes/delete_thread cycle so the inherited mutation and the snapshot are atomic. | `src/sessions/checkpoint.py`: lock changed to `RLock`, `with self._lock:` now wraps `super().put + _persist_state`, `super().put_writes + _persist_state`, and the `delete_thread` body. The inner lock in `_persist_state` and `_load_state` remains as defense-in-depth. |
| complete | Tests / checkpoint race | **Structural and behavioral tests for checkpoint lock scoping.** Behavioral race tests are timing-dependent and didn't reliably trigger the `RuntimeError` in this Python environment, so a structural test was added that asserts the invariant directly: `self._lock` must be held while the inherited `super().put` / `super().put_writes` runs. Verified the structural test catches a regression by reverting the fix (`assert [False, False] == [True, True]` fires immediately). | `tests/test_sessions.py`: `test_sqlite_memory_saver_serializes_writes_under_lock` (structural, monkey-patches `MemorySaver.put` to record `_lock._is_owned()` during the inherited call) and `test_sqlite_memory_saver_concurrent_writers_do_not_corrupt_state` (behavioral, 6 threads × tight loop). |
| complete | Verification | Full suite green after all three fixes. | `python -m pytest -q` → 181 passed (was 171 before today; +10 = 8 fallback tests + 2 checkpointer tests). |
| note | Web search / design | **Two-stage query rewriting taxonomy.** Common framings reduce to "refine vs extend" but the accurate map has four kinds: condensation (history → standalone Q, exists today in `condense` node), refinement (clean phrasing for the search engine, exists today in `src/web_search/query_prep.py`), expansion (multi-angle / synonym / HyDE rewrites for recall, **not implemented**), and decomposition (split compound Q into sub-Qs, **not implemented**). The first two are wired; the second two are the obvious growth points for retrieval recall. | Reference: `src/graph/nodes/common.py` `condense_question_factory`; `src/web_search/query_prep.py` `rewrite_search_query_llm` + mechanical fallback. |
| note | Web search / design | **Always-on expansion + decomposition node graph (sketched, not implemented).** Inserts three new nodes between `agent`'s tool call and `web_answer`: `decompose` (split compound Q into N sub-Qs, passthrough if atomic; LLM call, must be cheap on no-op), `expand` (per sub-Q produce k paraphrases / synonyms / HyDE; LLM call), and `merge` (deduplicate URLs across the N×k search results, rank by hit count + provider rank, keep top-K; pure code, no LLM). `web_search` ToolNode is reused, called M=N×k times in parallel. New nodes live as `src/graph/nodes/{decompose,expand,merge}.py` factories. Trade-off: pays the k× search cost on every query, even ones already well-formed. | Routing change is a single edit to `LIGHTWEIGHT_AGENT_EDGE_MAP` in `src/graph/edges.py` to send the `tools` branch to `decompose` instead of `web_search`. The agent-fallback from this same form's `web_answer` fix continues to work unchanged. |
| note | Web search / design | **Conditional-expansion variant (preferred, sketched, not implemented).** Same three new nodes, but `expand` runs only when the first single-query attempt failed at `web_answer`. Recovery order: single-query search → `web_answer` fails → `expand` + N×k search → `web_answer` retry → agent fallback (existing fix above) → END. Cheaper than always-on on the happy path (single query for well-formed Qs); strictly more expensive on worst case (two `web_answer` runs + agent fallback). One new state field (`expansion_attempted: bool`) acts as a one-shot switch. The change to `route_after_web_answer` is three outcomes instead of two: no readable content + not yet expanded → `expand`; no readable content + already expanded → `agent`; readable → `END`. | Trade-off depends on traffic mix: if most questions find their answer with a single keyword search (which the stable-facts category already does post-prompt-fix), conditional wins. Open design questions captured: (1) signal trigger — `web_answer`'s no-readable-content (accurate but pays one fetch) vs a pre-fetch `assess` node (cheap but URL-level signals are weak); (2) merge semantics — combine first-attempt URLs with expanded URLs, or rebuild from scratch. |
| complete | Web search / expansion | **Implemented the conditional-expansion variant: decompose → web_search → merge → web_answer chain with expand fallback.** The agent's `live_web_search` tool call now routes to `decompose` (LLM splits compound Q into 1-3 atomic sub-Qs, passthrough for already-atomic Qs), then `web_search` (existing ToolNode), then `merge` (dedupes + ranks URLs by hit count and provider rank, keeps top-K), then `web_answer` (unchanged). When `web_answer` produces no readable content and expansion hasn't been attempted, the post-web_answer edge routes to `expand` (LLM reformulates each sub-Q into k=3 keyword paraphrases), then through `web_search` → `merge` → `web_answer` a second time. A second failure routes to the agent fallback (existing fix above). Expansion is bounded by the one-shot `expansion_attempted` flag. | New `src/graph/nodes/decompose.py` (decompose LLM → 1-3 sub-Qs, passthrough for atomic), `src/graph/nodes/expand.py` (LLM → k=3 paraphrases per sub-Q, passthrough on failure), `src/graph/nodes/merge.py` (URL dedup by canonical form + ranking scan). State: `expansion_attempted: bool` in `src/graph/state.py`. Edges: `route_after_web_answer` extended to 3 outcomes (END / expand / agent) in `src/graph/edges.py`; `LIGHTWEIGHT_TOOL_EDGE_MAP` routes to `merge`; `LIGHTWEIGHT_AGENT_EDGE_MAP` routes to `decompose`; `WEB_ANSWER_FALLBACK_MAX_ATTEMPTS` bumped to 3. Builder: `src/graph/builder.py` registers `decompose`/`expand`/`merge` nodes and wires deterministic edges decompose→web_search, expand→web_search, merge→web_answer. `src/graph/nodes/__init__.py` exports new factories and constants. |
| complete | Tests / conditional expansion | **16 new unit tests for the three nodes + merge and decomp/expand fail-safe behavior.** | `tests/test_conditional_expansion_nodes.py`: 5 decompose tests (passthrough, LLM-fail, clamp-N, raw-output, dedup), 5 expand tests (no-sub-Qs, LLM-fail, clamp-k, flatten-across-subs, dedup-across-subs), 6 merge tests (canonical-dedup, query-string-preserve, combine-first+tool, top-k, hit-count-ranking, empty-inputs). |
| complete | Verification | Full suite green after conditional-expansion implementation. | `python -m pytest --tb=no` → 198 passed (was 181 before; +17 = 16 conditional-expansion node tests + 1 lightweight-graph integration test added by the orchestrator). `python -m compileall src tests` passed. |
| note | Coordination / design | The conditional-expansion variant is now implemented. The three design `note` rows above this form documented the plan (two-stage rewriting taxonomy, always-on sketch, conditional variant); this row closes the loop. Remaining open question for a future session: the `assess` pre-fetch signal alternative — the current implementation uses `web_answer`'s "no readable content" signal, which is accurate but pays one fetch before deciding to expand. Implementations: `src/graph/nodes/{decompose,expand,merge}.py` plus state/edges/builder/tests. | Cross-reference: the `expand.py` `_resolve_sub_questions` fallback fix (2026-06-17, part of this wave) guards `qa_question_resolver` against empty-state edge cases — the resolver does `messages[0]` without bounds check and raises `IndexError` on `{"messages": []}`. This was the fix that brought the suite from 1 failure to 198/198. |

## 2026-06-18 Form -- China-Locale Map Tools (Gaode / Baidu) + Coordinate Conversion

| Label | Scope | Line Item | Evidence / Next Action |
|---|---|---|---|
| note | Agent tools / design | **Rationale for adding China-locale map tools.** Existing agent toolset (`src/tools/{weather,stock,currency,wikipedia_tool}.py`) is global, no-key, and OSM-oriented. The user has flagged that addresses in mainland China are often sparse in OSM/Nominatim, and that Baidu/高德 provide materially better coverage of Chinese POIs, streets, districts, and adcodes. Project already has Baidu integration for **web search** (`src/web_search/baidu.py`, HTML scraping for `live_web_search`); this plan adds the **map/geocoding/routing** surface, which is a different service family. | Reference: the 2026-06-12 tool-expansion form. New work follows the same pattern (`build_<name>_tool(settings) → StructuredTool`, gated by `settings.<tool>_enabled`). |
| note | Agent tools / design | **Critical coordinate-system gotcha.** WGS-84 (OSM, Nominatim, OSRM, Open-Meteo), GCJ-02 (Gaode, Tencent, AutoNavi), and BD09 (Baidu) are three different coord systems. A WGS-84 point and a Baidu point for the same location differ by 50–500 meters. Any tool that consumes Gaode/Baidu output and feeds it to OSM/OSRM/Open-Meteo (or vice versa) **must convert**. The `coord.py` module is therefore not optional -- it is a precondition for `gaode.py` correctness. | The conversion is open-source deterministic math (no API call, no key); the canonical reference algorithm is the `eviltransform` family. |
| to do | Agent tools / coord | **`src/tools/coord.py`** -- WGS-84 ↔ GCJ-02 ↔ BD09 converter, pure Python, no I/O. Public API: `wgs84_to_gcj02(lat, lon)`, `gcj02_to_wgs84(lat, lon)`, `wgs84_to_bd09(lat, lon)`, `bd09_to_wgs84(lat, lon)`, `gcj02_to_bd09(lat, lon)`, `bd09_to_gcj02(lat, lon)`. Includes `is_in_china_bbox(lat, lon)` helper for routing decisions. Algorithm: standard GCJ-02 Krasovsky 1940 iteration + BD09 = GCJ-02 + delta. | New file `src/tools/coord.py`; functions return `(lat, lon)` tuples. Module is import-only (no `build_coord_tool()`); other tools import it. |
| to do | Agent tools / gaode | **`src/tools/gaode.py`** -- Gaode (高德) tool set using `https://restapi.amap.com/v3` and `v5/direction/driving`. Public surface mirrors the design discussed: `geocode(address, city)`, `reverse(location)`, `route_driving(origin, destination)`, `weather(city_code)`, `locate_ip(ip)`, `district(keywords, subdistrict)`. Plus a single `build_gaode_tool(settings)` factory that exposes a unified `gaode_map(action, **kwargs)` LangChain `StructuredTool` with Pydantic `args_schema` (action enum: `geocode`/`reverse`/`route_driving`/`weather`/`locate_ip`/`district`). All endpoints send `key=settings.gaode_api_key`, `output=JSON`. | New file `src/tools/gaode.py`; uses lazy `httpx` import pattern; uses `src/tools/coord.py` to keep `route_driving` results displayable in WGS-84 frames if needed. Read key from `GAODE_API_KEY` env. |
| to do | Config / settings | **Settings fields for Gaode tool.** Add to `src/config/settings.py`, `src/config/loader.py`, `config/default.yaml`, and `.env.example`: `gaode_enabled: bool = False`, `gaode_api_key: str = ""`, `gaode_locale_detection: bool = True`. | `gaode_locale_detection` toggles the Chinese-routing branch in the graph (see language-routing note below). When `False`, Gaode tool is available but never auto-selected. |
| to do | Agent tools / wiring | **Wire `gaode_map` into `_resolve_tools()` in `src/graph/builder.py`.** Position after the existing `weather`/`stock`/`currency`/`wikipedia` tools, gated by `settings.gaode_enabled`. | One edit in `src/graph/builder.py::_resolve_tools`; one `__init__.py` export in `src/tools/__init__.py`. |
| to do | Agent tools / prompt | **Update `AGENT_SYSTEM_PROMPT` in `src/llm/prompts.py`.** Add a `gaode_map` tool description: use for Chinese addresses, POI lookup, district hierarchy, IP location, and Chinese-weather fallbacks. Document the action enum and required `key=GAODE_API_KEY` requirement so the LLM doesn't call it without the key being set. | `src/llm/prompts.py`: append to the existing tool list. |
| to do | Graph / routing | **Locale-based tool-selection branch (optional, gated by `gaode_locale_detection`).** When a query contains Han characters (`[一-鿿]`) and `gaode_locale_detection=True`, the graph's `agent` node's system prompt should bias toward `gaode_map(geocode|route_driving|reverse)` for the first attempt; for non-Chinese queries the existing OSM/Photon/Nominatim path remains. Implementation: a small `detect_locale(question) -> Literal["zh", "intl"]` helper in `src/tools/locale.py`, and a per-turn dynamic system-prompt override in `src/graph/nodes/common.py` (already the home of `condense_question_factory`). | If too invasive, defer to a `note` and ship the tool first; the LLM can already be told via the prompt to prefer `gaode_map` on Chinese inputs. |
| to do | Tests / coord | **Unit tests for `coord.py`.** Round-trip accuracy: `wgs84_to_gcj02_to_wgs84` should be within 1e-6 deg (~0.1 m); `gcj02_to_bd09_to_gcj02` exact; known fixtures (Tiananmen Square WGS-84 `39.9087, 116.3975` → GCJ-02 ≈ `39.9101, 116.4034`); `is_in_china_bbox` for points inside/outside the 9-dash line bbox. | New `tests/test_coord.py` with 8-10 tests. |
| to do | Tests / gaode | **Unit tests for `gaode.py` builder and `build_gaode_tool`.** Mock `httpx` calls for each action; verify URL, params (`key`, `output=JSON`, action-specific), response shape passthrough, and graceful missing-key / missing-httpx behavior. | Extend `tests/test_agent_tools.py` (the existing home for the four tool builders from 2026-06-12) with 6-8 Gaode tests. |
| to do | Tests / config | **Config / env propagation tests.** Default `gaode_enabled=False`; env `GAODE_ENABLED=1` flips to true; missing `GAODE_API_KEY` raises only when tool is built; `gaode_locale_detection` defaults to `True` and env-overridable. | Extend `tests/test_config.py` with 3-4 tests. |
| to do | Tests / wiring | **Graph builder gating test.** When `gaode_enabled=False`, `gaode_map` is not in the tool list; when `True`, it is appended after the four existing tools. | Extend `tests/test_graph_builder.py` with 1-2 tests. |
| to do | Docs | **README + `.env.example` updates.** Add `GAODE_ENABLED`, `GAODE_API_KEY`, `GAODE_LOCALE_DETECTION` to the optional-settings table; brief note on coordinate-system difference and the fact that the `coord.py` helper handles conversion automatically inside Gaode tool code. | `README.md` settings table; `.env.example` new section. |
| to do | Verification | **Full verification pass after the wave.** | `python -m pytest -q` should reach ~210+ tests (198 today + ~12 new); `python -m compileall src tests`; `python -m ruff check ...`; `python -m pip check`. |
| note | Future / out of scope | **Baidu Map tool (`baidu_map`) not planned in this wave.** The project already has Baidu for web search (HTML scrape), but a separate Baidu Maps tool would duplicate the Gaode surface and add a second coordinate system (BD09). If Gaode is insufficient for a specific Chinese use case, prefer adding it in a follow-up form rather than expanding this one. | Reference: 2026-06-12 form tool-expansion pattern; 2026-06-09 form Baidu web-search integration. |
| note | Future / out of scope | **Self-hosted OSRM and Mapbox / Geoapify / OpenRouteService** were considered as additions to the routing path. They are deferred -- the OSRM demo is good enough for prototype traffic, and Gaode's `route_driving` covers the production case for Chinese users. If/when a non-Chinese routing use case materializes, the OSM+OSRM tool from the earlier design notes is the natural fit. | Cross-reference: the `live_web_search` and `weather` tools share the same `StructuredTool.from_function()` pattern that `gaode_map` will use. |
| note | Future / out of scope | **Static map image generation** (URL builder for `restapi.amap.com/v3/staticmap` or `staticmap.openstreetmap.de`) is intentionally **not** part of this tool. Returning an image URL is straightforward, but the agent has no UI to render it. Defer until a chat-side renderer exists. | Open follow-up if the project ever gains an image-rendering chat panel. |

## 2026-06-15 Form -- Readiness Docs Repair

| Label | Scope | Line Item | Evidence / Next Action |
|---|---|---|---|
| complete | Docs / env | Removed stale merge conflict markers from `.env.example` around `DEEPSEEK_API_KEY`. | Verify with `rg -n "<<<<<<<|=======|>>>>>>>" .env.example SECURITY.md CONTRIBUTING.md README.md`. |
| complete | Security docs | Updated `SECURITY.md` to reflect implemented optional API-key auth and configurable CORS. | Auth remains open when `API_KEY` is unset; shared deployments should configure `API_KEY` and scoped CORS origins. |
| complete | Dev docs | Updated `CONTRIBUTING.md` to reflect configured Ruff, MyPy, pre-commit, and coverage checks. | Verify whitespace with `git diff --check` for the touched docs/env files. |
| blocked | Verification | Full source/test verification was intentionally not run for this docs-only worker scope. | User requested conflict-marker and whitespace checks only; no source, tests, requirements, or CI files were edited. |

## 2026-06-15 Form -- Worker C API Ruff/Mypy Slice

| Label | Scope | Line Item | Evidence / Next Action |
|---|---|---|---|
| complete | API / Ruff | Fixed Ruff issues in owned FastAPI/API paths. | `src/api`, `src/qa`, and `src/chat` now use sorted imports, `| None` annotations, and FastAPI `Annotated` dependency aliases instead of `Depends(...)` defaults. |
| complete | API / mypy | Reduced owned-path mypy issues without changing API behavior. | Added route/helper return annotations, typed SSE iterators, response-model field completion, app-state casts, and a typed FastAPI exception-handler adapter. |
| blocked | mypy / imports | Remaining mypy errors are in non-owned imported modules. | Current blockers are outside Worker C ownership: `src/errors.py`, `src/config`, `src/web_search`, `src/tools`, `src/sessions`, `src/llm`, `src/graph`, `src/rag`, and `src/core`. |
| to do | Verification | Re-run final Ruff, mypy, and targeted API tests after this form update. | Commands: `.\.venv\Scripts\ruff.exe check src/api src/qa src/chat`, `.\.venv\Scripts\mypy.exe src/api src/qa src/chat`, and focused `tests/test_api_*` where available. |

## 2026-06-15 Form -- Worker A Core Ruff/Mypy Slice

| Label | Scope | Line Item | Evidence / Next Action |
|---|---|---|---|
| complete | Core / Ruff | Fixed Ruff issues in owned non-API paths. | `.\.venv\Scripts\ruff.exe check src/errors.py src/utils src/config src/web_search src/rag src/sessions src/graph src/llm` passed. |
| complete | Core / mypy | Fixed owned-slice mypy errors without changing RAG behavior. | `.\.venv\Scripts\mypy.exe src/errors.py src/utils src/config src/web_search src/rag src/sessions src/graph src/llm` passed after isolating lazy tool imports and typing third-party boundaries. |
| complete | Core / tests | Ran focused tests for the owned slice. | `.\.venv\Scripts\pytest.exe tests\test_config.py tests\test_graph_builder.py tests\test_graph_executor.py tests\test_graph_state_and_nodes.py tests\test_rag_interfaces.py tests\test_retry.py tests\test_sessions.py tests\test_urls.py tests\test_web_search_lightweight_primitives.py tests\test_web_search_providers.py -q` passed with 131 tests. |
| blocked | Verification | Full `tests -q` verification did not complete in this worker turn. | `.\.venv\Scripts\pytest.exe tests -q` timed out after 120s while running; focused owned-slice tests completed successfully. |
| note | Coordination | Other workers have active edits outside Worker A ownership. | Left API/chat/qa/docs/env changes untouched; Worker A edits are limited to assigned core paths plus this required process form. |

## 2026-06-12 Form — Multi-Tool Agent Expansion Plan

| Label | Scope | Line Item | Evidence / Next Action |
|---|---|---|---|
| note | Agent tools | **Plan: Add 4 new LangChain tools to the agent.** The agent currently has `retrieve_source_documents` (Chroma) and `live_web_search` (URLs only). Adding Weather, Stock, Currency, and Wikipedia tools makes the agent capable of answering real-time factual questions without needing the user to provide URLs. All target APIs are free, require no API key, and follow the existing `StructuredTool.from_function()` pattern. | Architecture: new `src/tools/` package. Each tool = one module with `build_<name>_tool(settings)` → `BaseTool`. Added to `_resolve_tools()` in `src/graph/builder.py` behind a `settings.<tool>_enabled` flag. Config/default/env wiring for each tool toggle. |
| note | Coordination | Spawned RAGRefactorDeveloper for the 2026-06-12 tool-expansion pass. | Agent `Nietzsche` is reviewing this form, code dependencies, and implementation order while parent work proceeds. |
| complete | Agent tools / weather | **Weather Forecast Tool** — `build_weather_tool()` using Open-Meteo API (`https://api.open-meteo.com/v1/forecast`). No API key, global coverage, current conditions + daily forecast up to 16 days. | Added `src/tools/weather.py` with city geocoding, coordinate support, formatted current conditions, and mocked unit coverage in `tests/test_agent_tools.py`. Settings: `weather_enabled: bool = False`. |
| complete | Agent tools / stock | **Stock Quote Tool** — `build_stock_tool()` using `yfinance` library. Free, no API key, global market coverage via Yahoo Finance. | Added `src/tools/stock.py` with lazy `yfinance` import, injected ticker factory for tests, formatted price/change/range/volume/market-cap output, and graceful missing-dependency/upstream errors. Settings: `stock_enabled: bool = False`. |
| complete | Agent tools / currency | **Currency Converter Tool** — `build_currency_tool()` using Frankfurter API (`https://api.frankfurter.dev/v2/rate`). Public no-key API with 201 currencies. | Added `src/tools/currency.py`; switched away from exchangerate.host after RAGRefactorDeveloper review because Frankfurter's current docs explicitly preserve no-key public access. Input: amount, from_currency, to_currency. Settings: `currency_enabled: bool = False`. |
| complete | Agent tools / wikipedia | **Wikipedia Search Tool** — `build_wikipedia_tool()` using MediaWiki API (`https://en.wikipedia.org/w/api.php`). Free, no API key, rate-limited by User-Agent. | Added `src/tools/wikipedia_tool.py`; uses search then extracts for summaries, returns page URLs, and sends configurable `User-Agent`/`Api-User-Agent`. Settings: `wikipedia_enabled: bool = False`, `wikipedia_max_summary_chars: int = 1500`, `wikipedia_user_agent`. |
| complete | Agent tools / wiring | Wired all new tools into `_resolve_tools()` in `src/graph/builder.py`, gated behind `settings.<tool>_enabled` flags. | Tools appear after `retrieve_source_documents` and optional `live_web_search` in the main RAG graph. Lightweight web-search graph intentionally remains URL-only because its answer node expects web URLs, not arbitrary tool text. |
| complete | Agent tools / config | Added new Settings fields and env-var mappings for all tool toggles. | `src/config/settings.py`, `src/config/loader.py`, `config/default.yaml`, and `.env.example` now include `WEATHER_ENABLED`, `STOCK_ENABLED`, `CURRENCY_ENABLED`, `WIKIPEDIA_ENABLED`, `WIKIPEDIA_MAX_SUMMARY_CHARS`, and `WIKIPEDIA_USER_AGENT`. `.env.example` fake DeepSeek-looking key was replaced with a placeholder. |
| complete | Agent tools / tests | Added focused tests for each tool builder and graph/config gating. | `tests/test_agent_tools.py` covers mocked Weather, Stock, Currency, and Wikipedia outputs. `tests/test_graph_builder.py` covers gated graph resolution. `tests/test_config.py` covers defaults/env coercion. |
| complete | Agent tools / prompt | Updated `AGENT_SYSTEM_PROMPT` to describe the optional tools and when to use them. | `src/llm/prompts.py` now lists `get_weather`, `get_stock_quote`, `convert_currency`, and `search_wikipedia` alongside retriever/web-search guidance. |
| complete | Agent tools / deps | Added `yfinance` to requirements. | `requirements.txt`: added `yfinance==1.4.1` after checking the current PyPI release. Installed it into `.venv`, then repaired the pinned requirements stack and confirmed `pip check` is clean. |
| complete | Agent tools / verification | Ran full verification after all tool implementations. | `.\.venv\Scripts\python.exe -m pytest -q` passed; `.\.venv\Scripts\python.exe -m ruff check ...` passed; `.\.venv\Scripts\python.exe -m compileall src tests` passed; `.\.venv\Scripts\python.exe -m pip check` passed; `git diff --check` passed with line-ending warnings only. |
| note | Agent tools / future | Follow-up tools after the initial 4: ArXiv academic paper search, GitHub repo search, built-in calculator (no API needed), and news headlines. | These follow the same pattern and can be added after the initial wave is verified. Calculator is the simplest — pure Python, no network, always-on. |
| note | Agent tools / design | Each tool follows the `live_web_search` pattern: `build_<name>_tool(settings) → StructuredTool` with Pydantic `args_schema`, clear name, and descriptive docstring that helps the LLM decide when to call it. Tools return formatted text (not raw JSON) because the graph's generate node consumes tool output as context text. | Reference: `src/web_search/tool.py` for the canonical pattern. |

## 2026-06-10 Form

| Label | Scope | Line Item | Evidence / Next Action |
|---|---|---|---|
| complete | LLM / provider | Added `DeepSeekLLMProvider` using `ChatOpenAI` pointed at `api.deepseek.com`. | `src/llm/provider.py`: `DeepSeekLLMProvider.chat_model()` constructs `ChatOpenAI(model=deepseek_model, base_url=deepseek_base_url)`. `build_chat_model()` dispatches on `settings.llm_provider`. |
| complete | Config / settings | Added `llm_provider`, `deepseek_api_key`, `deepseek_model`, `deepseek_base_url` fields. | `src/config/settings.py`: `llm_provider="dashscope"`, `deepseek_model="deepseek-v4-pro"`, `deepseek_base_url="https://api.deepseek.com"`. |
| complete | Config / loader | Added `DEEPSEEK_API_KEY`, `DEEPSEEK_MODEL`, `LLM_PROVIDER` env vars + provider-key validation. | `src/config/loader.py`: `SETTING_ENV_NAMES` updated; `load_settings()` raises when `llm_provider=deepseek` and `DEEPSEEK_API_KEY` is missing; `apply_runtime_environment()` exports `DEEPSEEK_API_KEY`. |
| complete | Config / YAML | Added new keys to default config. | `config/default.yaml`: `llm_provider: dashscope`, `deepseek_model: deepseek-v4-pro`, `deepseek_base_url: https://api.deepseek.com`. |
| complete | Config / .env | Added new env vars to example file. | `.env.example`: added `LLM_PROVIDER`, `DEEPSEEK_API_KEY`, `DEEPSEEK_MODEL`. |
| complete | API / readiness | Updated readiness checks to validate active provider's API key. | `src/api/dependencies.py`: added `_llm_api_key_configured()` helper; QA/chat readiness now include `llm_api_key_configured` check. |
| complete | Dependencies | Added `langchain-openai==0.3.17`. | `requirements.txt`: added `langchain-openai==0.3.17`. |
| complete | Docs | Updated README, root SKILL.md, graph SKILL.md model references. | `README.md`: added `LLM_PROVIDER`/`DEEPSEEK_API_KEY` to settings table and updated Notes. `SKILL.md`: updated LLM Provider row. `src/graph/SKILL.md`: updated LLM row. |
| complete | Tests | Added 3 DeepSeek config tests: provider settings loading, missing-key error, dashscope default. | `tests/test_config.py`: `test_load_settings_deepseek_provider`, `test_load_settings_deepseek_requires_api_key`, `test_load_settings_defaults_to_dashscope`. |
| complete | Verification | Full test suite green. | `python -m pytest -q` → 144 passed (was 134, +10 from new config tests and existing test fixes). Smoke test confirmed `build_chat_model()` constructs `ChatTongyi` (dashscope) by default. |
| note | DeepSeek | DeepSeek API key integration testing blocked on having a valid key and the model available. | Once you add `LLM_PROVIDER=deepseek` and `DEEPSEEK_API_KEY=<key>` to `.env`, the chat model will switch automatically. Verify with `python -c "from src.config.loader import load_settings; from src.llm.provider import build_chat_model; s=load_settings(); print(build_chat_model(s))"`. |
| note | Web search / quality | Identified 5 gaps in the deterministic filtering pipeline (Stages 1-4) that can be strengthened without adding external API dependencies. | The pipeline is currently rule-based only: URL noise patterns, pre-index text quality checks, lexical re-ranking, and a readability guard. Five concrete improvements identified below — ordered by impact-to-effort ratio. |
| complete | Stage 2 / dedup | **Gap 3: Content deduplication.** Tracks SHA256 hashes of normalized document text in `filter_quality_documents()` and skips duplicates. | `src/rag/document_quality.py`: added normalized lowercase content hashing and `duplicate_content` rejection accounting. Covered by `test_document_quality_filter_skips_normalized_duplicate_documents`. |
| complete | Stage 2 / semantic | **Gap 1: Embedding similarity gate.** Added cosine-similarity relevance gating against the query embedding, reusing the Chroma build embedding instance. | `src/rag/document_quality.py`, `src/rag/document_loader.py`, `src/rag/chroma_retriever.py`: query plus first 800 document chars are embedded when `document_quality_relevance_query` is set and embeddings are available; keyword overlap remains fallback. Added `document_quality_min_similarity=0.5` config. |
| complete | Stage 1 / scoring | **Gap 2: Configurable URL score threshold.** Exposed the URL quality threshold as `web_search_min_url_score` and added DEBUG score logging for each usable candidate. | `src/web_search/common.py`, `src/web_search/discovery.py`, `src/config/settings.py`, `src/config/loader.py`, `config/default.yaml`, `.env.example`: default remains 45, env var is `WEB_SEARCH_MIN_URL_SCORE`, and focused tests cover filtering/logging. |
| complete | Stage 3 / re-rank | **Gap 4: Embedding-based re-ranking option.** Added optional `RERANK_STRATEGY=lexical|embedding|hybrid`; lexical remains the default. | `src/graph/nodes/common.py`: embedding and hybrid paths score chunks against query embeddings with lexical fallback on provider failure. Config/default/env docs and graph/config tests updated. |
| complete | Stage 2 / recency | **Gap 5: Date extraction from HTML.** Extracts publication dates from `article:published_time` meta tags, `<time>` tags, JSON-LD, and date metadata; biases recent pages first. | Added `src/web_search/date_extractor.py`; `DateAwareWebBaseLoader` preserves publication dates in metadata; `filter_quality_documents()` annotates recency score and sorts recent documents higher. Added `document_quality_recency_bias_days=365` config and focused tests. |
| complete | Verification | Stage 2 document-quality slice verified. | `python -m pytest -q tests\test_rag_interfaces.py tests\test_config.py` passed (40 tests); `python -m pytest -q` passed (153 tests); `python -m compileall src tests` passed; `git diff --check` passed with line-ending warnings only. |
| note | Web search / quality | All five planned 2026-06-10 deterministic filtering/re-ranking gaps are implemented in the working tree. | Parent integration owns final full-suite verification and any cleanup after subagent merge review. |
| complete | Verification | Parent integrated and verified the 2026-06-10 quality wave. | `.venv\Scripts\python.exe -m pytest -q` passed (153 tests); `.venv\Scripts\python.exe -m compileall src tests` passed; `git diff --check` passed with line-ending warnings only. |
| complete | Web search / tool description | **Fix 1:** Updated `live_web_search` tool description to instruct LLM to formulate keyword queries (not NL questions) and include current year for time-sensitive queries. | `src/web_search/tool.py`: description now includes examples and explicit keyword-formulation instructions. |
| complete | Web search / query preprocessing | **Fix 2 + 3:** Added `prepare_search_query()` that strips question filler words and appends current year. Wired into `_discover_with_provider()` so all search engine queries are preprocessed. | New `src/web_search/query_prep.py`; `src/web_search/discovery.py` imports and calls it before each search provider call. |
| complete | Web search / URL relevance | **Fix 4:** Added query-term relevance bonus to `url_quality_score()`. URLs whose hostname/path contain query keywords get up to +20 extra points. Threaded via `select_top_urls()` and `ranked_usable_urls()`. | `src/web_search/common.py`: `url_quality_score()` now accepts `query=` keyword arg; `_url_query_relevance_bonus()` scores hostname (+8) and path segments (+5 each, max +20); `select_top_urls()` and `ranked_usable_urls()` pass `query=` through. |
| complete | LLM / prompts | **Fix 6:** Added semantic-matching instructions to both the lightweight web-answer prompt and the heavy `RAG_PROMPT`. Instructs the LLM to match on meaning, not exact phrasing — bridging vocabulary gaps like "latest model" ↔ "V4 Pro release 2026". | `src/web_search/prompt_builder.py`: added "IMPORTANT — Semantic matching" block; `src/llm/prompts.py`: same instruction in `RAG_PROMPT`. |
| complete | Web search / refusal message | **Fix 5:** Updated `web_answer` refusal message to suggest keyword-based queries and year inclusion when no readable pages are found. | `src/graph/nodes/web_answer.py`: refusal message now suggests "Try using specific keywords instead of a full question" with example. |
| complete | Tests | Added 7 focused tests: `prepare_search_query` unit tests (5) + URL relevance scoring tests (2). Updated 4 existing discovery fallback tests to expect preprocessed queries with appended year. | `tests/test_web_search_providers.py`: `test_prepare_search_query_*` (5 tests), `test_url_quality_score_relevance_bonus`, `test_select_top_urls_passes_query_through`. Updated 4 fallback test expected queries. |
| complete | Verification | Full test suite green after all 6 fixes. | `python -m pytest -q` → 160 passed (was 153, +7 new tests); `python -m compileall src tests` passed. |
| complete | Web search / LLM rewrite | **Proactive LLM query rewrite.** Added `rewrite_search_query_llm()` using DeepSeek V4 Flash (`deepseek-v4-flash`) to rewrite user questions into keyword search queries before calling the search engine. Gracefully falls back to mechanical `prepare_search_query()` when API key is missing or LLM is unavailable. | `src/web_search/query_prep.py`: `rewrite_search_query_llm()` with `_llm` injection for testing, `build_search_query()` as the unified entry point (LLM → mechanical fallback). `src/web_search/discovery.py`: `_discover_with_provider()` now calls `build_search_query(question, settings)`. Smoke tested: "what is the latest model of deepseek" → "DeepSeek latest model 2026". |
| complete | Tests / LLM rewrite | Added 3 tests: skip-when-no-api-key, returns-rewritten-query (with fake LLM), build_search_query integration. | `tests/test_web_search_providers.py`: `test_rewrite_search_query_llm_skips_when_no_api_key`, `test_rewrite_search_query_llm_returns_rewritten_query`, `test_build_search_query_uses_llm_when_available`. |
| complete | Verification | Full test suite green after LLM rewrite. | `python -m pytest -q` → 163 passed (was 160, +3 new tests); `python -m compileall src tests` passed. |

## 2026-06-09 Form (web-answer grounding + date-aware prompts)

| Label | Scope | Line Item | Evidence / Next Action |
|---|---|---|---|
| complete | Web search fetch policy | Added optional JS-capable fallback and domain fetch policy for Problem 3A/3E. | Added `src/web_search/fetch_policy.py` and `src/web_search/playwright_loader.py`; wired `fetch_pages()` to apply browser-like HTTP headers for policy hosts, skip HTTP for force-JS domains, and retry low-text policy domains once through a lazy Playwright Chromium adapter when `web_search_js_fallback_enabled` is true. |
| complete | Tests | Added focused JS fallback policy coverage. | Extended `tests/test_web_search_lightweight_primitives.py`, `tests/test_lightweight_graph_web_answer_integration.py`, and `tests/test_config.py` for low-text JS retry, force-JS skip, fallback failure preservation, domain matching, and settings/env/default propagation. |
| complete | Verification | Focused web-search/config tests and touched-file lint pass. | Ran `.venv\Scripts\python.exe -m pytest tests\test_web_search_lightweight_primitives.py tests\test_lightweight_graph_web_answer_integration.py tests\test_config.py` (35 passed, 2 warnings) and `.venv\Scripts\python.exe -m ruff check ...` (all checks passed). |
| complete | Web answer / source priority | Fixed `web_answer` choosing the agent's redundant in-graph `live_web_search` URLs over the session's curated discovered URLs. | Reordered `_extract_source_urls` in `src/graph/nodes/web_answer.py` to prefer explicit `state["source_urls"]`, then curated `settings.source_urls`, then tool-message URLs as a fallback. Root cause of the DeepSeek query fetching only 2 junk timed-out URLs (`deepseek.net/es`, `deep-seek.com/es`) while ignoring the 6 quality-filtered session URLs. |
| complete | Web answer / grounding | Stopped ungrounded answers when no page yields readable text. | `src/graph/nodes/web_answer.py` now skips the LLM call entirely when every fetched page is empty/unreachable and returns a grounded refusal listing the attempted URLs, instead of prompting with empty context and letting the model answer from stale training data. |
| complete | Prompts / date awareness | Fixed answers defaulting to ~2024 training-cutoff knowledge. | Added `Today's date is <date>` plus explicit source-trust instructions to both the lightweight web prompt (`src/web_search/prompt_builder.py`) and the heavy `RAG_PROMPT` (`src/llm/prompts.py`). `RAG_PROMPT` gained a `{current_date}` variable bound via `RAG_PROMPT.partial(current_date=date.today().isoformat())` in `src/graph/nodes/common.py`'s generate node. `date` derived from `datetime.date.today()`. |
| complete | Prompts / token budget | Prevented the longer instructional header from starving source content. | `build_web_search_prompt` now excludes the fixed instructional preamble from the per-source token budget; only the question block plus source sections are charged against `max_total_tokens`. |
| complete | Tests | Updated and added regression tests for the new behavior. | Updated `tests/test_lightweight_graph_web_answer_integration.py` (tool URLs now fallback-only) and `tests/test_web_search_lightweight_primitives.py` (budget asserts source trimming, not total length). Added regression tests: curated settings URLs beat tool URLs, and no-readable-pages returns a grounded refusal without calling the LLM. |
| complete | Verification | Ran full suite after the web-answer/prompt fixes. | `.venv\Scripts\python.exe -m pytest -p no:warnings` → 130 passed; `python -m compileall src tests` passed. |
| note | Web answer | The agent still runs a redundant in-graph `live_web_search` each turn even when the session already has curated URLs. | Now harmless (its results no longer win), but wasteful; a future optimization could skip the in-graph search when the session already carries quality-filtered source URLs. |

## 2026-06-09 Form

| Label | Scope | Line Item | Evidence / Next Action |
|---|---|---|---|
| complete | Web search lightweight path | Implemented Phase A.1/A.2/A.7 primitives. | Added `src/web_search/content_fetcher.py` and `src/web_search/prompt_builder.py`, exported `FetchedPage`, `fetch_pages`, and `build_web_search_prompt` from `src/web_search/__init__.py`, and added focused coverage in `tests/test_web_search_lightweight_primitives.py`; verification: `python -m pytest -q tests/test_web_search_lightweight_primitives.py`, `python -m pytest -q tests/test_web_search_providers.py tests/test_web_search_lightweight_primitives.py`, and scoped `python -m compileall ...` passed. |
| note | Coordination | Spawned RAGRefactorDeveloper subagents for the first implementation wave. | Ada Lovelace (`019eaa2c-67f9-7823-8862-9daf50bb86f2`, tool nickname Avicenna) owns chat typed-404 test fix; Isaac Newton (`019eaa2c-b1cf-77b2-99d8-9c4a313e779e`, tool nickname Dewey) owns SQLiteMemorySaver compatibility; Grace Hopper (`019eaa2d-1517-7502-baad-e4d8a8726788`, tool nickname Goodall) owns concurrent URL loading in `src/rag/document_loader.py`. |
| note | Coordination | Parent thread owns process documentation while subagents work. | Do not let parallel workers edit `memory/current-process.md` or `memory/refactor-daily-forms.md`; parent will update these files from each worker's final changed-file and verification summary to avoid documentation conflicts. |
| complete | Coordination | Integrated first-wave subagent results in dependency order. | Ada and Isaac baseline test fixes were integrated first, then Grace's URL-loading performance patch; parent verification passed before later cache, quality, runtime tool, and re-ranking waves started. |
| complete | Tests / Chat API | Ada Lovelace fixed the unknown-thread typed 404 regression. | Changed `src/chat/api.py`; root cause was FastAPI resolving config/graph-lock dependencies before the missing session lookup, causing 503 to win over typed 404. Worker verification: `python -m pytest -q tests/test_api_errors.py::test_chat_unknown_thread_uses_typed_not_found_response` passed, `python -m pytest -q tests/test_api_errors.py` passed with 9 tests, and `python -m pytest -q tests/test_api_streaming.py` passed with 2 tests; parent full-suite verification passed after integration. |
| complete | Tests / Sessions | Isaac Newton fixed `SQLiteMemorySaver` compatibility with the installed LangGraph saver. | Changed `src/sessions/checkpoint.py`; root cause was assuming a `blobs` map exists on `MemorySaver`. Worker verification: `python -m pytest -q tests/test_sessions.py::test_sqlite_memory_saver_restores_graph_state_after_reopen` passed and `python -m pytest -q tests/test_sessions.py` passed with 15 tests; parent full-suite verification passed after integration. |
| complete | Web search performance | Grace Hopper parallelized source URL loading. | Changed `src/rag/document_loader.py` and `tests/test_rag_interfaces.py`; `load_source_documents()` now uses bounded `ThreadPoolExecutor` loading with `max_concurrent_loads=4`, keeps failed-URL behavior, and preserves returned document order by input URL. Worker verification: `python -m pytest -q tests\test_rag_interfaces.py` passed with 11 tests; `ruff` was unavailable; parent full-suite verification passed after integration. |
| complete | Verification | Parent integrated and verified first-wave subagent work. | `python -m pytest -q` passed with 87 tests; `python -m compileall src tests` passed; `git diff --check` passed with line-ending warnings only. |
| note | Coordination | Spawned RAGRefactorDeveloper subagents for the second implementation wave. | Marie Curie (`019eaa35-994d-7d51-ae8a-335b20751962`, tool nickname Faraday) owns config/default alignment and URL-load concurrency settings; Katherine Johnson (`019eaa35-eb26-7760-aee8-57d1e8650fba`, tool nickname Dirac) owns runtime LangChain web-search tool wiring; Emmy Noether (`019eaa36-450f-78e2-8669-8379f5d8a2ee`, tool nickname Feynman) owns provider-result quality filtering/fallback. |
| complete | Web search config | Marie Curie aligned web-search defaults and plumbed URL-load concurrency through settings. | Changed `src/config/settings.py`, `src/config/loader.py`, `config/default.yaml`, `.env.example`, `src/rag/chroma_retriever.py`, `tests/test_config.py`, and `tests/test_rag_interfaces.py`; canonical `web_search_top_k` is now 6 and `page_load_max_concurrency` defaults to 4. Worker verification: config/RAG focused tests passed with 18 tests, `git diff --check -- ...` passed with line-ending warnings only, and full `python -m pytest -q` passed with 95 tests; parent integration verification passed after the full second wave. |
| complete | Web search tool | Katherine Johnson exposed runtime web search as a LangChain tool. | Added `src/web_search/tool.py`, exported it from `src/web_search/__init__.py`, wired `src/graph/builder.py` to append `live_web_search` when `settings.web_search_enabled` is true, and added tests in `tests/test_graph_builder.py` and `tests/test_web_search_providers.py`. Worker verification: focused graph/web-search tests passed with 22 tests and full `python -m pytest -q` passed with 97 tests; `ruff` was unavailable; parent integration verification passed after the full second wave. |
| complete | Web search quality | Emmy Noether added deterministic provider-result quality gates and fallback. | Changed `src/web_search/common.py`, `src/web_search/discovery.py`, and `tests/test_web_search_providers.py`; filters low-value search/login/tag/file/download/feed/about-style URLs, scores URL candidates, deduplicates by canonical host/path, and treats all-filtered provider output as a miss so fallback can continue. Worker verification: `python -m pytest -q tests\test_web_search_providers.py` passed with 17 tests; parent integration verification passed after the full second wave. |
| complete | Verification | Parent integrated and verified second-wave subagent work. | `python -m pytest -q` passed with 97 tests; `python -m compileall src tests` passed; `git diff --check` passed with line-ending warnings only. |
| note | Coordination | Spawned RAGRefactorDeveloper subagents for the third implementation wave. | Nikola Tesla (`019eaa40-493f-7752-a151-2920bd7aba43`, tool nickname Averroes) owns in-process source document caching; Rosalind Franklin (`019eaa40-98a3-7001-9d3e-14997bece7e7`, tool nickname Lorentz) owns Bing timelimit/recency support. |
| complete | Web search performance | Nikola Tesla added opt-in in-process source document caching. | Changed `src/rag/document_loader.py`, `src/config/settings.py`, `src/config/loader.py`, `src/rag/chroma_retriever.py`, `config/default.yaml`, `.env.example`, `tests/test_rag_interfaces.py`, and `tests/test_config.py`; `page_load_cache_ttl_seconds` defaults to 0/off, caches only successful non-empty loads by `(url, timeout)`, deep-copies documents on store/read, and deduplicates concurrent duplicate misses with per-key locks. Worker verification: config/RAG focused tests passed with 24 tests and full `python -m pytest -q` passed; parent integration verification passed after the full third wave. |
| complete | Web search provider | Rosalind Franklin added Bing timelimit/recency support. | Changed `src/web_search/bing.py`, `src/web_search/factory.py`, and `tests/test_web_search_providers.py`; maps `d/day`, `w/week`, and `m/month` to Bing `filters` values while unsupported values preserve default behavior. Worker verification: `python -m pytest -q tests\test_web_search_providers.py` passed with 21 tests; parent integration verification passed after the full third wave. |
| complete | Verification | Parent integrated and verified third-wave subagent work. | `python -m pytest -q` passed with 107 tests; `python -m compileall src tests` passed; `git diff --check` passed with line-ending warnings only. |
| note | Coordination | Spawned RAGRefactorDeveloper subagents for the final quality wave. | Lise Meitner (`019eaa4a-b12f-7352-8568-01fe4e67eff7`, tool nickname Archimedes) owns pre-index document content quality/relevance filtering; Alan Turing (`019eaa4b-07d4-7692-b7a1-39d178d97523`, tool nickname Erdos) owns deterministic post-retrieval re-ranking. |
| complete | Web search quality | Lise Meitner added pre-index document quality filtering. | Added `src/rag/document_quality.py` and changed `src/rag/document_loader.py`, `src/rag/chroma_retriever.py`, config files, `.env.example`, and focused tests; loaded docs are filtered before split/embed for empty/short text, low text signal, repeated-line boilerplate, boilerplate-heavy text, low unique-term content, and optional query keyword overlap. Worker verification: config/RAG focused tests passed with 28 tests, full `python -m pytest -q` passed with 113 tests, and `git diff --check ...` had only CRLF warnings; parent integration verification passed after the full final wave. |
| complete | Web search quality | Alan Turing added deterministic post-retrieval document re-ranking. | Changed `src/graph/nodes/common.py` and `tests/test_graph_state_and_nodes.py`; retrieved chunks are re-ranked before grading and generation using local query/document token overlap, token frequency, and phrase bonuses, with ties preserving original retrieval order. Worker verification: graph state/node tests passed with 9 tests and graph state/executor tests passed with 14 tests; parent integration verification passed after the full final wave. |
| complete | Verification | Parent integrated and verified final quality-wave subagent work. | `python -m pytest -q` passed with 113 tests; `python -m compileall src tests` passed; `git diff --check` passed with line-ending warnings only. |
| complete | Documentation | Synced README with the final web-search settings and verification state. | Updated README optional settings from `WEB_SEARCH_TOP_K=3` to `WEB_SEARCH_TOP_K=6`, added `PAGE_LOAD_MAX_CONCURRENCY`, `PAGE_LOAD_CACHE_TTL_SECONDS`, and `DOCUMENT_QUALITY_FILTER_ENABLED`, and updated latest integrated verification from 74 tests to 113 tests. Final parent verification still passed with 113 tests, compileall passed, and `git diff --check` had line-ending warnings only. |
| complete | Web search | Investigated why web search parsing is slow and answers are sometimes incorrect. | Root causes documented in `PROBLEMS_DETECTED.md`; two categories found: (1) sequential URL loading + full re-embedding + Chroma rebuild makes the pipeline take minutes, (2) no content quality scoring or re-ranking — search engine rank is the sole gate for what enters the vector store. |
| complete | Web search | Reviewed `docs/code-review-web-search.md` findings. | Confirmed web search is not exposed as a LangChain tool — the agent cannot search the live web at runtime, only query its static Chroma index; this is a critical missing capability tracked alongside the performance/quality issues. |
| complete | Documentation | Created `PROBLEMS_DETECTED.md` with 12 root-cause findings. | Covers 6 performance bottlenecks (sequential URL loading, 15s per-URL timeout, no HTTP caching, full re-embedding, full Chroma rebuild, no lazy loading) and 6 quality gaps (no content scoring, no re-ranking, basic noise filter, no Bing recency filter, no quality-based fallback, config drift on `web_search_top_k`). |
| complete | Documentation | Updated `memory/current-process.md` to reflect current project state. | Branch: `web-developed` (19 commits ahead of main); tests: 85 total (83 passed, 2 failing); web search: 3 providers, known performance/quality issues documented; config drift noted for `web_search_top_k`. |
| complete | Verification | Ran full test suite to get current baseline. | `python -m pytest -q` result: 85 tests total — 83 passed, 2 failed (`test_chat_unknown_thread_uses_typed_not_found_response` → 503 vs 404, `test_sqlite_memory_saver_restores_graph_state_after_reopen` → `SQLiteMemorySaver` missing `blobs` attribute). |
| note | Test status | 2 tests regressed from the previous all-green state (was 74/74). | `test_chat_unknown_thread_uses_typed_not_found_response` likely fails due to missing service dependency in test; `test_sqlite_memory_saver_restores_graph_state_after_reopen` is a LangGraph version incompatibility — `SQLiteMemorySaver` interface changed (`blobs` attribute removed in newer LangGraph). |
| note | Web search | Web search pipeline overview for future fix planning. | Flow: `discover_urls_from_web()` → search engine → `select_top_urls()` → `build_graph(rebuild=True)` → `load_source_documents()` (sequential, per-URL 15s timeout) → `split_documents()` → `Chroma.from_documents()` (full embed) → agent → retrieve → grade → generate. The rebuild and embed phases are the main latency sources. |
| complete | Web search | Fixed web search performance: parallelized URL loading. | `load_source_documents()` now uses bounded `ThreadPoolExecutor` loading, preserves URL order, and exposes `page_load_max_concurrency`; verified in the 113-test suite. |
| complete | Web search | Fixed web search performance: added opt-in HTTP/content caching. | Added in-process source document cache with `page_load_cache_ttl_seconds` defaulting to 0/off; successful non-empty loads are cached by `(url, timeout)` and duplicate concurrent misses are deduplicated. |
| complete | Web search | Fixed answer quality: added pre-index content quality filtering. | Added deterministic loaded-document filtering for short/empty/boilerplate/low-signal/low-unique-term pages plus optional query overlap before splitting and embedding. |
| complete | Web search | Fixed answer quality: added deterministic re-ranking after retrieval. | Retrieved chunks are re-ranked before grading and generation using local lexical overlap/frequency/phrase scoring; no external model dependency added. |
| complete | Web search | Exposed web search as a LangChain tool for the agent. | Added `live_web_search` via `src/web_search/tool.py` and graph builder wiring so settings-based graphs include the runtime web-search tool when `web_search_enabled` is true. |
| complete | Tests | Fixed 2 failing tests and restored green baseline. | Fixed chat unknown-thread typed 404 ordering and `SQLiteMemorySaver` compatibility; final parent verification passed with 113 tests. |
| complete | Web search | Resolved config drift on `web_search_top_k`. | Canonical default is now 6 across `src/config/settings.py`, `config/default.yaml`, `.env.example`, and README; final parent verification passed. |

## 2026-06-08 Form

| Label | Scope | Line Item | Evidence / Next Action |
|---|---|---|---|
| note | Coordination | Started to-do completion pass with RAGRefactorDeveloper subagents. | Spawned one compatibility/endpoint review worker and one Phase 2/Phase 3 readiness worker; parent owns environment repair, test execution, and shared process checkpoints. |
| complete | Environment | Verified a working project Python runner. | `python --version` reports Python 3.11.7 and can import/run the installed test dependencies; `.venv\\Scripts\\python.exe` also reports Python 3.11.7 but lacks `pytest`, so use `python` for verification in this environment. |
| complete | Verification | Ran the full test suite. | `python -m pytest -q` passed: 44 tests green after compatibility regression coverage was added. |
| complete | Phase 2 cleanup | Locally checked compatibility shims and endpoint contracts. | `python -m compileall src tests` passed; direct import check confirmed legacy QA/chat model and graph/session exports still resolve. |
| complete | Phase 2 cleanup / compatibility | Reviewed scoped core/chat compatibility modules and QA/chat HTTP schemas. | Restored legacy `src.core.nodes` helper aliases `_question_tokens` and `_split_context_sentences`; added focused regression coverage in `tests/test_graph_state_and_nodes.py`; scoped tests and full suite passed. |
| complete | Phase 2 remaining | Decided whether Phase 2 config/YAML/error-code work should continue before Phase 3. | Continue a short Phase 2 completion pass before Phase 3 because YAML config, typed error codes, fuller typed event emission, and LLM provider DI are not complete. |
| complete | Phase 3 planning | Prepared SSE/session persistence/observability readiness plan. | Start Phase 3 only after the Phase 2 completion pass and `python -m pytest -q` are green; build SSE on typed events, persistence on config/checkpointer settings, and metrics on events/error codes. |
| complete | Phase 2 config | Completed YAML config support before Phase 3. | Added `config/default.yaml`, flat YAML loading, CLI `--config` support where applicable, and CLI > env > YAML > defaults tests. |
| complete | Phase 2 errors | Added typed RAG error hierarchy and FastAPI handlers. | See later Phase 2 errors checkpoint in this form for changed files and verification. |
| complete | Phase 2 events | Completed non-SSE typed graph event emission groundwork. | `src/graph/executor.py` now emits node_start/node_end/error/done plus retriever and grader summaries when inferable from chunks; SSE/API integration remains deferred. |
| complete | Phase 2 LLM DI | Added an LLM provider seam. | Added `src/llm/provider.py`; graph nodes now build chat models through `build_chat_model()` instead of constructing `ChatTongyi` directly. |
| note | Phase 3 readiness | Phase 3 remains gated on Phase 2 completion and green tests. | SSE, SQLite session persistence, metrics, incremental Chroma rebuilds, and deprecation warnings should come after the Phase 2 completion pass. |
| note | Coordination | Started Phase 2 completion and Phase 3 implementation pass. | Parent owns YAML config and final integration; RAGRefactorDeveloper subagents are assigned disjoint scopes for errors, events/metrics, and session persistence. |
| complete | Phase 2 config | Implemented YAML config support first. | Added `config/default.yaml`, flat YAML loading with CLI > env > YAML > built-in precedence, `--config` plumbing for QA/chat CLIs and app factories, and config precedence tests; `python -m pytest -q tests\test_config.py` passed. |
| complete | Phase 2 errors | Implemented typed error hierarchy and FastAPI handlers. | Added `src/errors.py`, `src/api/errors.py`, QA/chat handler registration, typed chat 404s, QA generic exception wrapping, and `tests/test_api_errors.py`; `python -m pytest -q tests\\test_api_errors.py tests\\test_api_dependencies.py`, `python -m compileall src tests`, `git diff --check`, and full `python -m pytest -q` passed. |
| complete | Phase 2 events / Phase 3 metrics | Completed typed event emission and metrics groundwork. | Added `src/graph/metrics.py`, event-driven metrics collection, and executor tests for event order, error events, retriever/grader summaries, and metrics; `python -m pytest -q` passed with 62 tests. |
| complete | Phase 3 SSE/API | Integrated typed graph events with SSE endpoints. | Added `POST /query/stream`, `POST /chat/{thread_id}/message/stream`, shared SSE serialization, and API streaming tests. |
| complete | Phase 3 sessions | Added optional session persistence storage groundwork. | Added `StorageBackend`, `SessionMetadata`, `InMemoryStorage`, and stdlib `SQLiteStorage` for metadata only; optional registry hooks preserve default in-memory behavior; `python -m pytest -q tests\\test_sessions.py` and full `python -m pytest -q` passed. |
| complete | Phase 3 sessions | Resolved full LangGraph checkpoint persistence without a new dependency. | Added `SQLiteMemorySaver`, which persists LangGraph `MemorySaver` checkpoint maps to SQLite; chat startup now restores metadata and checkpoint state. |
| complete | Phase 3 metrics | Added in-process metrics endpoint. | `GraphExecutor` records event-driven metrics and QA/chat apps expose `GET /metrics`; API stream tests verify metrics increment after streamed requests. |
| complete | Phase 3 deprecation | Added deprecation warnings for old compatibility import paths. | Added `src/_compat.py`, warnings in `src/core/*` and `src/chat/*` facades, and `tests/test_compat.py` coverage. |
| complete | Verification | Ran final integrated verification. | `python -m pytest -q` passed with 66 tests; `python -m compileall src tests` passed; `git diff --check` passed. |
| note | Company readiness | Started combined planning pass from `memory/current-process.md`, `memory/refactor-daily-forms.md`, and `COMPANY_READINESS_GAPS.md`. | Parent owns checkpoint persistence and integration; subagents may own independent CI/quality/container/docs/security slices. |
| complete | Current blocker | Implemented restart-safe chat checkpoint persistence without adding an unverified external dependency. | Added `SQLiteMemorySaver`, wired chat startup to SQLite metadata/checkpoint stores, restored persisted session metadata into runtime registry, and added restart-state tests. |
| complete | Company readiness / infra | Added CI, quality gates, dev requirements, Docker, and compose. | Added `.github/workflows/ci.yml`, Ruff/mypy/pytest-cov config in `pyproject.toml`, `requirements-dev.txt`, `.pre-commit-config.yaml`, `Dockerfile`, `docker-compose.yml`, and `.dockerignore`; marked `COMPANY_READINESS_GAPS.md` sections 1-3 complete. |
| complete | Company readiness / local tests | Kept local base pytest runnable while CI enforces coverage. | Coverage settings remain configured, but `--cov` enforcement moved to CI where `requirements-dev.txt` installs `pytest-cov`; local `python -m pytest -q` works without dev extras. |
| complete | Company readiness / verification | Resolved integration regressions from concurrent readiness work. | Fixed chat graph wrapper delegation when no explicit checkpointer is supplied; full local test suite is green. |
| complete | Company readiness / API security | Added API-key auth, CORS configuration, and readiness endpoint. | `API_KEY` unset keeps local mutation endpoints open; when set, QA/chat mutation endpoints require `Authorization: Bearer <key>`; QA/chat expose `/ready` using local app-state checks only; CORS is driven by `cors_allow_origins`/`CORS_ALLOW_ORIGINS`. |
| complete | Company readiness / docs | Added team documentation and updated readiness gap tracking. | Added `CONTRIBUTING.md`, `ARCHITECTURE.md`, `CHANGELOG.md`, and `SECURITY.md`; updated `COMPANY_READINESS_GAPS.md` sections 8-12 without editing source or tests. |
| complete | Company readiness / persistence docs | Documented Chroma/session backup and restore boundaries. | `ARCHITECTURE.md` covers `.chroma/`, `.chroma/chat/<thread_id>/`, optional SQLite session metadata backup, restore steps, and the metadata-only limitation. |
| complete | Company readiness / database migrations | Added SQLite session schema versioning. | `SQLiteStorage` now creates a `schema_version` table and test coverage verifies the current schema version. |
| to do | Company readiness / security | Finish remaining security implementation tasks. | Dependency scanning remains open; scoped `sk-` hardcoded-key scan found no matches; CORS configuration and API-key auth are implemented. |
| complete | Company readiness / environments | Added multi-environment config support. | Added `config/development.yaml`, `config/staging.yaml`, and `config/production.yaml`; default config loading now overlays `config/{RAG_ENV}.yaml`, defaulting to `development`. |
| complete | Company readiness / API verification | Ran targeted API/config hardening tests. | `python -m pytest -q tests\test_api_security_readiness.py tests\test_api_dependencies.py tests\test_api_streaming.py tests\test_config.py` passed with 16 tests. |
| complete | Company readiness / full verification | Full local verification is green. | `python -m pytest -q` passed with 74 tests; `python -m compileall src tests` passed; `git diff --check` passed with line-ending warnings only. |
| complete | Company readiness / README | Updated README for the current implementation state. | Replaced stale notebook-era notes with setup, config precedence, QA/chat endpoints, SSE, auth/CORS, metrics/readiness, Docker, verification, persistence, and remaining readiness gaps. |
| complete | Environment / dependency repair | Fixed the local `.venv` install path after pip initially reported no LangChain matches and then exposed stale 1.x package leftovers. | Reinstalled `requirements.txt` through `.\.venv\Scripts\python.exe -m pip install --upgrade --force-reinstall --index-url https://pypi.org/simple -r requirements.txt`, removed stray `langchain-classic`, `sse-starlette`, and `mcp`, patched `SQLiteMemorySaver` to persist LangGraph 0.6 `blobs`, then `pip check` and full `.venv` pytest passed. |
| complete | Environment / requirements | Updated `requirements.txt` to the verified dependency set. | Pinned direct dependencies to the versions installed in the clean `.venv`, added direct `PyYAML` and `requests`, confirmed `.\.venv\Scripts\python.exe -m pip install -r requirements.txt`, `pip check`, and full pytest are green. |
| complete | Verification | Ran documentation pass verification. | `git diff --check` completed without whitespace errors; Git reported line-ending warnings only. |

## 2026-06-07 Form

| Label | Scope | Line Item | Evidence / Next Action |
|---|---|---|---|
| complete | Phase 1 | Extraction baseline implemented. | Shared utilities, config split, prompt extraction, unified graph state, shared graph nodes, compatibility facades, and focused tests are present in the working tree. |
| complete | Phase 2 / web_search | Provider interface pass implemented. | `src/web_search/` contains protocol, Baidu/DDG providers, factory, discovery helpers, and `src/core/web_search.py` compatibility. |
| complete | Phase 2 / rag | Retrieval interface pass implemented. | `src/rag/` contains retriever/embedding protocols, `ChromaRetriever`, embedding modules, document loader, and core shims. |
| complete | Phase 2 / sessions | Session interface pass implemented. | `src/sessions/` contains models, registry, TTL cleanup, isolated Chroma cleanup, settings isolation, and chat compatibility. |
| complete | Phase 2 / graph | Unified graph interface pass implemented. | `src/graph/` contains unified builder, edges, events, executor, state, and shared nodes. Legacy QA/chat graph wrappers delegate to it. |
| complete | Phase 2 / api | App-state API dependency pass implemented. | `src/api/` contains dependency helpers and shared models. QA/chat APIs use app state instead of old module-level graph/session globals. |
| complete | Verification | Lightweight checks passed. | `git diff --check` passed. Bundled Python `compileall src tests` passed. Targeted scans found no old QA/chat API globals or runtime `print()` calls in refactored paths. |
| blocked | Verification | Full pytest cannot run in the current local Python setup. | Repair or recreate `.venv`; current launcher points to missing Python 3.11, bundled Python lacks `pytest`, and venv compiled `pydantic_core` is incompatible with bundled Python. |
| to do | Environment | Recreate the project Python environment. | Install a working Python matching the venv or rebuild `.venv`, then install requirements including pytest. |
| to do | Verification | Run the full test suite. | Run `python -m pytest -q` after environment repair; record failures and fixes in the next dated form. |
| to do | Phase 2 cleanup | Review compatibility shims and endpoint contracts after pytest passes. | Confirm old imports and QA/chat HTTP schemas still behave as promised through Phase 2. |
| to do | Phase 2 remaining | Decide whether to continue Phase 2 config/YAML/error-code work before Phase 3. | Compare against `REFACTORING_PLAN.md` Phase 2 exit criteria. |
| to do | Phase 3 planning | Prepare SSE/session persistence/observability work only after Phase 2 verification is green. | Add the next dated form before starting Phase 3 work. |
| note | Agent workflow | Future RAGRefactorDeveloper workers must update this file when they finish. | Add or update the current date form, mark completed lines, refresh to-do lines, and record blocked checks. |
