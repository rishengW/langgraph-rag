---
name: refactor-daily-forms
description: Dated refactoring process forms with complete, to-do, blocked, and note lines
metadata:
  type: project
  updated: 2026-06-09
---

# Refactor Daily Forms

This file is the single process checklist for completed work and next actions.
Each date gets one form. Every form line must start with one of these labels:

- `complete` - finished and verified as far as the current environment allows
- `to do` - planned next work
- `blocked` - cannot proceed until an external condition changes
- `note` - context that future agents should preserve

## Form Template

| Label | Scope | Line Item | Evidence / Next Action |
|---|---|---|---|
| complete |  |  |  |
| to do |  |  |  |
| blocked |  |  |  |
| note |  |  |  |

## 2026-06-09 Form (web-answer grounding + date-aware prompts)

| Label | Scope | Line Item | Evidence / Next Action |
|---|---|---|---|
| complete | Web answer / source priority | Fixed `web_answer` choosing the agent's redundant in-graph `live_web_search` URLs over the session's curated discovered URLs. | Reordered `_extract_source_urls` in `src/graph/nodes/web_answer.py` to prefer explicit `state["source_urls"]`, then curated `settings.source_urls`, then tool-message URLs as a fallback. Root cause of the DeepSeek query fetching only 2 junk timed-out URLs (`deepseek.net/es`, `deep-seek.com/es`) while ignoring the 6 quality-filtered session URLs. |
| complete | Web answer / grounding | Stopped ungrounded answers when no page yields readable text. | `src/graph/nodes/web_answer.py` now skips the LLM call entirely when every fetched page is empty/unreachable and returns a grounded refusal listing the attempted URLs, instead of prompting with empty context and letting the model answer from stale training data. |
| complete | Prompts / date awareness | Fixed answers defaulting to ~2024 training-cutoff knowledge. | Added `Today's date is <date>` plus explicit source-trust instructions to both the lightweight web prompt (`src/web_search/prompt_builder.py`) and the heavy `RAG_PROMPT` (`src/llm/prompts.py`). `RAG_PROMPT` gained a `{current_date}` variable bound via `RAG_PROMPT.partial(current_date=date.today().isoformat())` in `src/graph/nodes/common.py`'s generate node. `date` derived from `datetime.date.today()`. |
| complete | Prompts / token budget | Prevented the longer instructional header from starving source content. | `build_web_search_prompt` now excludes the fixed instructional preamble from the per-source token budget; only the question block plus source sections are charged against `max_total_tokens`. |
| complete | Tests | Updated and added regression tests for the new behavior. | Updated `tests/test_lightweight_graph_web_answer_integration.py` (tool URLs now fallback-only) and `tests/test_web_search_lightweight_primitives.py` (budget asserts source trimming, not total length). Added regression tests: curated settings URLs beat tool URLs, and no-readable-pages returns a grounded refusal without calling the LLM. |
| complete | Verification | Ran full suite after the web-answer/prompt fixes. | `.venv\Scripts\python.exe -m pytest -p no:warnings` → 130 passed; `python -m compileall src tests` passed. |
| note | Web answer | The agent still runs a redundant in-graph `live_web_search` each turn even when the session already has curated URLs. | Now harmless (its results no longer win), but wasteful; a future optimization could skip the in-graph search when the session already carries quality-filtered source URLs. |

## 2026-06-09 Form

| Label | Scope | Line Item | Evidence / Next Action |
|---|---|---|---|
| complete | Web search lightweight path | Implemented Phase A.1/A.2/A.7 primitives. | Added `src/web_search/content_fetcher.py` and `src/web_search/prompt_builder.py`, exported `FetchedPage`, `fetch_pages`, and `build_web_search_prompt` from `src/web_search/__init__.py`, and added focused coverage in `tests/test_web_search_lightweight_primitives.py`; verification: `python -m pytest -q tests/test_web_search_lightweight_primitives.py`, `python -m pytest -q tests/test_web_search_providers.py tests/test_web_search_lightweight_primitives.py`, and scoped `python -m compileall ...` passed. |
| note | Coordination | Spawned RAGRefactorDeveloper subagents for the first implementation wave. | Ada Lovelace (`019eaa2c-67f9-7823-8862-9daf50bb86f2`, tool nickname Avicenna) owns chat typed-404 test fix; Isaac Newton (`019eaa2c-b1cf-77b2-99d8-9c4a313e779e`, tool nickname Dewey) owns SQLiteMemorySaver compatibility; Grace Hopper (`019eaa2d-1517-7502-baad-e4d8a8726788`, tool nickname Goodall) owns concurrent URL loading in `src/rag/document_loader.py`. |
| note | Coordination | Parent thread owns process documentation while subagents work. | Do not let parallel workers edit `memory/current-process.md` or `memory/refactor-daily-forms.md`; parent will update these files from each worker's final changed-file and verification summary to avoid documentation conflicts. |
| complete | Coordination | Integrated first-wave subagent results in dependency order. | Ada and Isaac baseline test fixes were integrated first, then Grace's URL-loading performance patch; parent verification passed before later cache, quality, runtime tool, and re-ranking waves started. |
| complete | Tests / Chat API | Ada Lovelace fixed the unknown-thread typed 404 regression. | Changed `src/chat/api.py`; root cause was FastAPI resolving config/graph-lock dependencies before the missing session lookup, causing 503 to win over typed 404. Worker verification: `python -m pytest -q tests/test_api_errors.py::test_chat_unknown_thread_uses_typed_not_found_response` passed, `python -m pytest -q tests/test_api_errors.py` passed with 9 tests, and `python -m pytest -q tests/test_api_streaming.py` passed with 2 tests; parent full-suite verification passed after integration. |
| complete | Tests / Sessions | Isaac Newton fixed `SQLiteMemorySaver` compatibility with the installed LangGraph saver. | Changed `src/sessions/checkpoint.py`; root cause was assuming a `blobs` map exists on `MemorySaver`. Worker verification: `python -m pytest -q tests/test_sessions.py::test_sqlite_memory_saver_restores_graph_state_after_reopen` passed and `python -m pytest -q tests/test_sessions.py` passed with 15 tests; parent full-suite verification passed after integration. |
| complete | Web search performance | Grace Hopper parallelized source URL loading. | Changed `src/rag/document_loader.py` and `tests/test_rag_interfaces.py`; `load_source_documents()` now uses bounded `ThreadPoolExecutor` loading with `max_concurrent_loads=4`, keeps failed-URL behavior, and preserves returned document order by input URL. Worker verification: `python -m pytest -q tests\test_rag_interfaces.py` passed with 11 tests; `ruff` was unavailable; parent full-suite verification passed after integration. |
| complete | Verification | Parent integrated and verified first-wave subagent work. | `python -m pytest -q` passed with 87 tests; `python -m compileall src tests` passed; `git diff --check` passed with line-ending warnings only. |
| note | Coordination | Spawned RAGRefactorDeveloper subagents for the second implementation wave. | Marie Curie (`019eaa35-994d-7d51-ae8a-335b20751962`, tool nickname Faraday) owns config/default alignment and URL-load concurrency settings; Katherine Johnson (`019eaa35-eb26-7760-aee8-57d1e8650fba`, tool nickname Dirac) owns runtime LangChain web-search tool wiring; Emmy Noether (`019eaa36-450f-78e2-8669-8379f5d8a2ee`, tool nickname Feynman) owns provider-result quality filtering/fallback. |
| complete | Web search config | Marie Curie aligned web-search defaults and plumbed URL-load concurrency through settings. | Changed `src/config/settings.py`, `src/config/loader.py`, `config/default.yaml`, `.env.example`, `src/rag/chroma_retriever.py`, `tests/test_config.py`, and `tests/test_rag_interfaces.py`; canonical `web_search_top_k` is now 6 and `page_load_max_concurrency` defaults to 4. Worker verification: config/RAG focused tests passed with 18 tests, `git diff --check -- ...` passed with line-ending warnings only, and full `python -m pytest -q` passed with 95 tests; parent integration verification passed after the full second wave. |
| complete | Web search tool | Katherine Johnson exposed runtime web search as a LangChain tool. | Added `src/web_search/tool.py`, exported it from `src/web_search/__init__.py`, wired `src/graph/builder.py` to append `live_web_search` when `settings.web_search_enabled` is true, and added tests in `tests/test_graph_builder.py` and `tests/test_web_search_providers.py`. Worker verification: focused graph/web-search tests passed with 22 tests and full `python -m pytest -q` passed with 97 tests; `ruff` was unavailable; parent integration verification passed after the full second wave. |
| complete | Web search quality | Emmy Noether added deterministic provider-result quality gates and fallback. | Changed `src/web_search/common.py`, `src/web_search/discovery.py`, and `tests/test_web_search_providers.py`; filters low-value search/login/tag/file/download/feed/about-style URLs, scores URL candidates, deduplicates by canonical host/path, and treats all-filtered provider output as a miss so fallback can continue. Worker verification: `python -m pytest -q tests\test_web_search_providers.py` passed with 17 tests; parent integration verification passed after the full second wave. |
| complete | Verification | Parent integrated and verified second-wave subagent work. | `python -m pytest -q` passed with 97 tests; `python -m compileall src tests` passed; `git diff --check` passed with line-ending warnings only. |
| note | Coordination | Spawned RAGRefactorDeveloper subagents for the third implementation wave. | Nikola Tesla (`019eaa40-493f-7752-a151-2920bd7aba43`, tool nickname Averroes) owns in-process source document caching; Rosalind Franklin (`019eaa40-98a3-7001-9d3e-14997bece7e7`, tool nickname Lorentz) owns Bing timelimit/recency support. |
| complete | Web search performance | Nikola Tesla added opt-in in-process source document caching. | Changed `src/rag/document_loader.py`, `src/config/settings.py`, `src/config/loader.py`, `src/rag/chroma_retriever.py`, `config/default.yaml`, `.env.example`, `tests/test_rag_interfaces.py`, and `tests/test_config.py`; `page_load_cache_ttl_seconds` defaults to 0/off, caches only successful non-empty loads by `(url, timeout)`, deep-copies documents on store/read, and deduplicates concurrent duplicate misses with per-key locks. Worker verification: config/RAG focused tests passed with 24 tests and full `python -m pytest -q` passed; parent integration verification passed after the full third wave. |
| complete | Web search provider | Rosalind Franklin added Bing timelimit/recency support. | Changed `src/web_search/bing.py`, `src/web_search/factory.py`, and `tests/test_web_search_providers.py`; maps `d/day`, `w/week`, and `m/month` to Bing `filters` values while unsupported values preserve default behavior. Worker verification: `python -m pytest -q tests\test_web_search_providers.py` passed with 21 tests; parent integration verification passed after the full third wave. |
| complete | Verification | Parent integrated and verified third-wave subagent work. | `python -m pytest -q` passed with 107 tests; `python -m compileall src tests` passed; `git diff --check` passed with line-ending warnings only. |
| note | Coordination | Spawned RAGRefactorDeveloper subagents for the final quality wave. | Lise Meitner (`019eaa4a-b12f-7352-8568-01fe4e67eff7`, tool nickname Archimedes) owns pre-index document content quality/relevance filtering; Alan Turing (`019eaa4b-07d4-7692-b7a1-39d178d97523`, tool nickname Erdos) owns deterministic post-retrieval re-ranking. |
| complete | Web search quality | Lise Meitner added pre-index document quality filtering. | Added `src/rag/document_quality.py` and changed `src/rag/document_loader.py`, `src/rag/chroma_retriever.py`, config files, `.env.example`, and focused tests; loaded docs are filtered before split/embed for empty/short text, low text signal, repeated-line boilerplate, boilerplate-heavy text, low unique-term content, and optional query keyword overlap. Worker verification: config/RAG focused tests passed with 28 tests, full `python -m pytest -q` passed with 113 tests, and `git diff --check ...` had only CRLF warnings; parent integration verification passed after the full final wave. |
| complete | Web search quality | Alan Turing added deterministic post-retrieval document re-ranking. | Changed `src/graph/nodes/common.py` and `tests/test_graph_state_and_nodes.py`; retrieved chunks are re-ranked before grading and generation using local query/document token overlap, token frequency, and phrase bonuses, with ties preserving original retrieval order. Worker verification: graph state/node tests passed with 9 tests and graph state/executor tests passed with 14 tests; parent integration verification passed after the full final wave. |
| complete | Verification | Parent integrated and verified final quality-wave subagent work. | `python -m pytest -q` passed with 113 tests; `python -m compileall src tests` passed; `git diff --check` passed with line-ending warnings only. |
| complete | Documentation | Synced README with the final web-search settings and verification state. | Updated README optional settings from `WEB_SEARCH_TOP_K=3` to `WEB_SEARCH_TOP_K=6`, added `PAGE_LOAD_MAX_CONCURRENCY`, `PAGE_LOAD_CACHE_TTL_SECONDS`, and `DOCUMENT_QUALITY_FILTER_ENABLED`, and updated latest integrated verification from 74 tests to 113 tests. Final parent verification still passed with 113 tests, compileall passed, and `git diff --check` had line-ending warnings only. |
| complete | Web search | Investigated why web search parsing is slow and answers are sometimes incorrect. | Root causes documented in `PROBLEMS_DETECTED.md`; two categories found: (1) sequential URL loading + full re-embedding + Chroma rebuild makes the pipeline take minutes, (2) no content quality scoring or re-ranking — search engine rank is the sole gate for what enters the vector store. |
| complete | Web search | Reviewed `docs/code-review-web-search.md` findings. | Confirmed web search is not exposed as a LangChain tool — the agent cannot search the live web at runtime, only query its static Chroma index; this is a critical missing capability tracked alongside the performance/quality issues. |
| complete | Documentation | Created `PROBLEMS_DETECTED.md` with 12 root-cause findings. | Covers 6 performance bottlenecks (sequential URL loading, 15s per-URL timeout, no HTTP caching, full re-embedding, full Chroma rebuild, no lazy loading) and 6 quality gaps (no content scoring, no re-ranking, basic noise filter, no Bing recency filter, no quality-based fallback, config drift on `web_search_top_k`). |
| complete | Documentation | Updated `memory/current-process.md` to reflect current project state. | Branch: `web-developed` (19 commits ahead of main); tests: 85 total (83 passed, 2 failing); web search: 3 providers, known performance/quality issues documented; config drift noted for `web_search_top_k`. |
| complete | Verification | Ran full test suite to get current baseline. | `python -m pytest -q` result: 85 tests total — 83 passed, 2 failed (`test_chat_unknown_thread_uses_typed_not_found_response` → 503 vs 404, `test_sqlite_memory_saver_restores_graph_state_after_reopen` → `SQLiteMemorySaver` missing `blobs` attribute). |
| note | Test status | 2 tests regressed from the previous all-green state (was 74/74). | `test_chat_unknown_thread_uses_typed_not_found_response` likely fails due to missing service dependency in test; `test_sqlite_memory_saver_restores_graph_state_after_reopen` is a LangGraph version incompatibility — `SQLiteMemorySaver` interface changed (`blobs` attribute removed in newer LangGraph). |
| note | Web search | Web search pipeline overview for future fix planning. | Flow: `discover_urls_from_web()` → search engine → `select_top_urls()` → `build_graph(rebuild=True)` → `load_source_documents()` (sequential, per-URL 15s timeout) → `split_documents()` → `Chroma.from_documents()` (full embed) → agent → retrieve → grade → generate. The rebuild and embed phases are the main latency sources. |
| complete | Web search | Fixed web search performance: parallelized URL loading. | `load_source_documents()` now uses bounded `ThreadPoolExecutor` loading, preserves URL order, and exposes `page_load_max_concurrency`; verified in the 113-test suite. |
| complete | Web search | Fixed web search performance: added opt-in HTTP/content caching. | Added in-process source document cache with `page_load_cache_ttl_seconds` defaulting to 0/off; successful non-empty loads are cached by `(url, timeout)` and duplicate concurrent misses are deduplicated. |
| complete | Web search | Fixed answer quality: added pre-index content quality filtering. | Added deterministic loaded-document filtering for short/empty/boilerplate/low-signal/low-unique-term pages plus optional query overlap before splitting and embedding. |
| complete | Web search | Fixed answer quality: added deterministic re-ranking after retrieval. | Retrieved chunks are re-ranked before grading and generation using local lexical overlap/frequency/phrase scoring; no external model dependency added. |
| complete | Web search | Exposed web search as a LangChain tool for the agent. | Added `live_web_search` via `src/web_search/tool.py` and graph builder wiring so settings-based graphs include the runtime web-search tool when `web_search_enabled` is true. |
| complete | Tests | Fixed 2 failing tests and restored green baseline. | Fixed chat unknown-thread typed 404 ordering and `SQLiteMemorySaver` compatibility; final parent verification passed with 113 tests. |
| complete | Web search | Resolved config drift on `web_search_top_k`. | Canonical default is now 6 across `src/config/settings.py`, `config/default.yaml`, `.env.example`, and README; final parent verification passed. |

## 2026-06-08 Form

| Label | Scope | Line Item | Evidence / Next Action |
|---|---|---|---|
| note | Coordination | Started to-do completion pass with RAGRefactorDeveloper subagents. | Spawned one compatibility/endpoint review worker and one Phase 2/Phase 3 readiness worker; parent owns environment repair, test execution, and shared process checkpoints. |
| complete | Environment | Verified a working project Python runner. | `python --version` reports Python 3.11.7 and can import/run the installed test dependencies; `.venv\\Scripts\\python.exe` also reports Python 3.11.7 but lacks `pytest`, so use `python` for verification in this environment. |
| complete | Verification | Ran the full test suite. | `python -m pytest -q` passed: 44 tests green after compatibility regression coverage was added. |
| complete | Phase 2 cleanup | Locally checked compatibility shims and endpoint contracts. | `python -m compileall src tests` passed; direct import check confirmed legacy QA/chat model and graph/session exports still resolve. |
| complete | Phase 2 cleanup / compatibility | Reviewed scoped core/chat compatibility modules and QA/chat HTTP schemas. | Restored legacy `src.core.nodes` helper aliases `_question_tokens` and `_split_context_sentences`; added focused regression coverage in `tests/test_graph_state_and_nodes.py`; scoped tests and full suite passed. |
| complete | Phase 2 remaining | Decided whether Phase 2 config/YAML/error-code work should continue before Phase 3. | Continue a short Phase 2 completion pass before Phase 3 because YAML config, typed error codes, fuller typed event emission, and LLM provider DI are not complete. |
| complete | Phase 3 planning | Prepared SSE/session persistence/observability readiness plan. | Start Phase 3 only after the Phase 2 completion pass and `python -m pytest -q` are green; build SSE on typed events, persistence on config/checkpointer settings, and metrics on events/error codes. |
| complete | Phase 2 config | Completed YAML config support before Phase 3. | Added `config/default.yaml`, flat YAML loading, CLI `--config` support where applicable, and CLI > env > YAML > defaults tests. |
| complete | Phase 2 errors | Added typed RAG error hierarchy and FastAPI handlers. | See later Phase 2 errors checkpoint in this form for changed files and verification. |
| complete | Phase 2 events | Completed non-SSE typed graph event emission groundwork. | `src/graph/executor.py` now emits node_start/node_end/error/done plus retriever and grader summaries when inferable from chunks; SSE/API integration remains deferred. |
| complete | Phase 2 LLM DI | Added an LLM provider seam. | Added `src/llm/provider.py`; graph nodes now build chat models through `build_chat_model()` instead of constructing `ChatTongyi` directly. |
| note | Phase 3 readiness | Phase 3 remains gated on Phase 2 completion and green tests. | SSE, SQLite session persistence, metrics, incremental Chroma rebuilds, and deprecation warnings should come after the Phase 2 completion pass. |
| note | Coordination | Started Phase 2 completion and Phase 3 implementation pass. | Parent owns YAML config and final integration; RAGRefactorDeveloper subagents are assigned disjoint scopes for errors, events/metrics, and session persistence. |
| complete | Phase 2 config | Implemented YAML config support first. | Added `config/default.yaml`, flat YAML loading with CLI > env > YAML > built-in precedence, `--config` plumbing for QA/chat CLIs and app factories, and config precedence tests; `python -m pytest -q tests\test_config.py` passed. |
| complete | Phase 2 errors | Implemented typed error hierarchy and FastAPI handlers. | Added `src/errors.py`, `src/api/errors.py`, QA/chat handler registration, typed chat 404s, QA generic exception wrapping, and `tests/test_api_errors.py`; `python -m pytest -q tests\\test_api_errors.py tests\\test_api_dependencies.py`, `python -m compileall src tests`, `git diff --check`, and full `python -m pytest -q` passed. |
| complete | Phase 2 events / Phase 3 metrics | Completed typed event emission and metrics groundwork. | Added `src/graph/metrics.py`, event-driven metrics collection, and executor tests for event order, error events, retriever/grader summaries, and metrics; `python -m pytest -q` passed with 62 tests. |
| complete | Phase 3 SSE/API | Integrated typed graph events with SSE endpoints. | Added `POST /query/stream`, `POST /chat/{thread_id}/message/stream`, shared SSE serialization, and API streaming tests. |
| complete | Phase 3 sessions | Added optional session persistence storage groundwork. | Added `StorageBackend`, `SessionMetadata`, `InMemoryStorage`, and stdlib `SQLiteStorage` for metadata only; optional registry hooks preserve default in-memory behavior; `python -m pytest -q tests\\test_sessions.py` and full `python -m pytest -q` passed. |
| complete | Phase 3 sessions | Resolved full LangGraph checkpoint persistence without a new dependency. | Added `SQLiteMemorySaver`, which persists LangGraph `MemorySaver` checkpoint maps to SQLite; chat startup now restores metadata and checkpoint state. |
| complete | Phase 3 metrics | Added in-process metrics endpoint. | `GraphExecutor` records event-driven metrics and QA/chat apps expose `GET /metrics`; API stream tests verify metrics increment after streamed requests. |
| complete | Phase 3 deprecation | Added deprecation warnings for old compatibility import paths. | Added `src/_compat.py`, warnings in `src/core/*` and `src/chat/*` facades, and `tests/test_compat.py` coverage. |
| complete | Verification | Ran final integrated verification. | `python -m pytest -q` passed with 66 tests; `python -m compileall src tests` passed; `git diff --check` passed. |
| note | Company readiness | Started combined planning pass from `memory/current-process.md`, `memory/refactor-daily-forms.md`, and `COMPANY_READINESS_GAPS.md`. | Parent owns checkpoint persistence and integration; subagents may own independent CI/quality/container/docs/security slices. |
| complete | Current blocker | Implemented restart-safe chat checkpoint persistence without adding an unverified external dependency. | Added `SQLiteMemorySaver`, wired chat startup to SQLite metadata/checkpoint stores, restored persisted session metadata into runtime registry, and added restart-state tests. |
| complete | Company readiness / infra | Added CI, quality gates, dev requirements, Docker, and compose. | Added `.github/workflows/ci.yml`, Ruff/mypy/pytest-cov config in `pyproject.toml`, `requirements-dev.txt`, `.pre-commit-config.yaml`, `Dockerfile`, `docker-compose.yml`, and `.dockerignore`; marked `COMPANY_READINESS_GAPS.md` sections 1-3 complete. |
| complete | Company readiness / local tests | Kept local base pytest runnable while CI enforces coverage. | Coverage settings remain configured, but `--cov` enforcement moved to CI where `requirements-dev.txt` installs `pytest-cov`; local `python -m pytest -q` works without dev extras. |
| complete | Company readiness / verification | Resolved integration regressions from concurrent readiness work. | Fixed chat graph wrapper delegation when no explicit checkpointer is supplied; full local test suite is green. |
| complete | Company readiness / API security | Added API-key auth, CORS configuration, and readiness endpoint. | `API_KEY` unset keeps local mutation endpoints open; when set, QA/chat mutation endpoints require `Authorization: Bearer <key>`; QA/chat expose `/ready` using local app-state checks only; CORS is driven by `cors_allow_origins`/`CORS_ALLOW_ORIGINS`. |
| complete | Company readiness / docs | Added team documentation and updated readiness gap tracking. | Added `CONTRIBUTING.md`, `ARCHITECTURE.md`, `CHANGELOG.md`, and `SECURITY.md`; updated `COMPANY_READINESS_GAPS.md` sections 8-12 without editing source or tests. |
| complete | Company readiness / persistence docs | Documented Chroma/session backup and restore boundaries. | `ARCHITECTURE.md` covers `.chroma/`, `.chroma/chat/<thread_id>/`, optional SQLite session metadata backup, restore steps, and the metadata-only limitation. |
| complete | Company readiness / database migrations | Added SQLite session schema versioning. | `SQLiteStorage` now creates a `schema_version` table and test coverage verifies the current schema version. |
| to do | Company readiness / security | Finish remaining security implementation tasks. | Dependency scanning remains open; scoped `sk-` hardcoded-key scan found no matches; CORS configuration and API-key auth are implemented. |
| complete | Company readiness / environments | Added multi-environment config support. | Added `config/development.yaml`, `config/staging.yaml`, and `config/production.yaml`; default config loading now overlays `config/{RAG_ENV}.yaml`, defaulting to `development`. |
| complete | Company readiness / API verification | Ran targeted API/config hardening tests. | `python -m pytest -q tests\test_api_security_readiness.py tests\test_api_dependencies.py tests\test_api_streaming.py tests\test_config.py` passed with 16 tests. |
| complete | Company readiness / full verification | Full local verification is green. | `python -m pytest -q` passed with 74 tests; `python -m compileall src tests` passed; `git diff --check` passed with line-ending warnings only. |
| complete | Company readiness / README | Updated README for the current implementation state. | Replaced stale notebook-era notes with setup, config precedence, QA/chat endpoints, SSE, auth/CORS, metrics/readiness, Docker, verification, persistence, and remaining readiness gaps. |
| complete | Environment / dependency repair | Fixed the local `.venv` install path after pip initially reported no LangChain matches and then exposed stale 1.x package leftovers. | Reinstalled `requirements.txt` through `.\.venv\Scripts\python.exe -m pip install --upgrade --force-reinstall --index-url https://pypi.org/simple -r requirements.txt`, removed stray `langchain-classic`, `sse-starlette`, and `mcp`, patched `SQLiteMemorySaver` to persist LangGraph 0.6 `blobs`, then `pip check` and full `.venv` pytest passed. |
| complete | Environment / requirements | Updated `requirements.txt` to the verified dependency set. | Pinned direct dependencies to the versions installed in the clean `.venv`, added direct `PyYAML` and `requests`, confirmed `.\.venv\Scripts\python.exe -m pip install -r requirements.txt`, `pip check`, and full pytest are green. |
| complete | Verification | Ran documentation pass verification. | `git diff --check` completed without whitespace errors; Git reported line-ending warnings only. |

## 2026-06-07 Form

| Label | Scope | Line Item | Evidence / Next Action |
|---|---|---|---|
| complete | Phase 1 | Extraction baseline implemented. | Shared utilities, config split, prompt extraction, unified graph state, shared graph nodes, compatibility facades, and focused tests are present in the working tree. |
| complete | Phase 2 / web_search | Provider interface pass implemented. | `src/web_search/` contains protocol, Baidu/DDG providers, factory, discovery helpers, and `src/core/web_search.py` compatibility. |
| complete | Phase 2 / rag | Retrieval interface pass implemented. | `src/rag/` contains retriever/embedding protocols, `ChromaRetriever`, embedding modules, document loader, and core shims. |
| complete | Phase 2 / sessions | Session interface pass implemented. | `src/sessions/` contains models, registry, TTL cleanup, isolated Chroma cleanup, settings isolation, and chat compatibility. |
| complete | Phase 2 / graph | Unified graph interface pass implemented. | `src/graph/` contains unified builder, edges, events, executor, state, and shared nodes. Legacy QA/chat graph wrappers delegate to it. |
| complete | Phase 2 / api | App-state API dependency pass implemented. | `src/api/` contains dependency helpers and shared models. QA/chat APIs use app state instead of old module-level graph/session globals. |
| complete | Verification | Lightweight checks passed. | `git diff --check` passed. Bundled Python `compileall src tests` passed. Targeted scans found no old QA/chat API globals or runtime `print()` calls in refactored paths. |
| blocked | Verification | Full pytest cannot run in the current local Python setup. | Repair or recreate `.venv`; current launcher points to missing Python 3.11, bundled Python lacks `pytest`, and venv compiled `pydantic_core` is incompatible with bundled Python. |
| to do | Environment | Recreate the project Python environment. | Install a working Python matching the venv or rebuild `.venv`, then install requirements including pytest. |
| to do | Verification | Run the full test suite. | Run `python -m pytest -q` after environment repair; record failures and fixes in the next dated form. |
| to do | Phase 2 cleanup | Review compatibility shims and endpoint contracts after pytest passes. | Confirm old imports and QA/chat HTTP schemas still behave as promised through Phase 2. |
| to do | Phase 2 remaining | Decide whether to continue Phase 2 config/YAML/error-code work before Phase 3. | Compare against `REFACTORING_PLAN.md` Phase 2 exit criteria. |
| to do | Phase 3 planning | Prepare SSE/session persistence/observability work only after Phase 2 verification is green. | Add the next dated form before starting Phase 3 work. |
| note | Agent workflow | Future RAGRefactorDeveloper workers must update this file when they finish. | Add or update the current date form, mark completed lines, refresh to-do lines, and record blocked checks. |
