[project]
name = "langgraph-rag"
version = "0.1.0"
description = "Local LangGraph RAG application"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"

