# Development and quality-gate dependencies.
# Runtime dependencies remain in requirements.txt.
# ruff is pinned to the same version as .pre-commit-config.yaml so local
# hooks and CI formatting agree.
pytest>=8.0,<9
pytest-cov>=5.0,<7
pytest-timeout>=2.3,<3
ruff==0.8.6
mypy>=1.14,<2
pre-commit>=4.0,<5
httpx>=0.27,<1
hypothesis>=6.100,<7
types-PyYAML>=6.0,<7
